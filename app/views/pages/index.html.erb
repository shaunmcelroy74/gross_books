<p style="color: green"><%= notice %></p>

<% content_for :title, "Pages" %>

<h1 class="title is-2">Pages</h1>

<div id="pages">
  <% @pages.each do |page| %>
    <%= render page %>
    <p>
      <%= link_to "Show this page", page %>
    </p>
  <% end %>
</div>

<%= link_to "New page", new_page_path %> |
<%= link_to "Contact", pages_permalink_path("contact") %>
